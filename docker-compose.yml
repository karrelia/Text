services:
  bot:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      DB_PATH: /app/data/bot.db
    volumes:
      - ./data:/app/data
    # Потрібні лише для MODE=webhook — за polling можна закоментувати.
    ports:
      - "127.0.0.1:8080:8080"
